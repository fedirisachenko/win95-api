// app-wide — timing constants shared between `match` and `search` flows.
// Place feature-only timings under apps/matchmaking/src/<feature>/constant/.
export const ACCEPT_TTL_SECONDS = 60;
export const ACCEPT_TIMEOUT_SECONDS = 30 * 50;
export const CHAT_READY_TIMEOUT_SECONDS = 30 * 50;
